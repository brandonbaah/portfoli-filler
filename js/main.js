$(document).ready(function() {

$(function(){
                $(".likes").typed({
                    strings: ["EARLY MORNING GYM RAT", "BASKETBALL COMMENTATOR", "FRONT END NINJA", "PHP WARRIOR", "RUBY REBEL"],
                    typeSpeed: 0,
                    showCursor: false,
                    loop: true,
                    loopCount: false,
                    backDelay: 1000
                });
            });
});